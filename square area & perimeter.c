#include<stdio.h>
void main()
{
	int a,b,c,d;
	printf("eneter all number to multiply");
	scanf("%d%d%d%d",  &a, &b, &c, &d);
	printf("product of %d, %d, %d and %d is %d", a, b, c, d, a*b*c*d);
}
